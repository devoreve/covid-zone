export const config = {
    services: {
        iq: {
            api_key: 'Votré clé API (je vais quand même pas vous filer la mienne !)'
        }
    },
    data: {
        default: {
            lat: 48.57,
            lng: 7.75,
            zoom: 15,
            radius: 1000
        }
    }
};